# Pinned to what this project was built and benchmarked against.
# CPU-only by design: there is no CUDA path on Apple Silicon, and the envs are
# small enough that multi-process CPU stepping beats anything else available here.
mujoco==3.11.0
gymnasium==1.3.0
stable-baselines3[extra]==2.9.0
torch==2.13.0
numpy==2.5.1

# Portable policies. ONNX is the interchange format precisely because it is data
# for a fixed operator set rather than a pickle - an uploaded .zip would be
# arbitrary code execution. onnxscript is required by torch 2.13's exporter.
onnx==1.22.0
onnxruntime==1.28.0
onnxscript==0.7.1

fastapi==0.141.1
uvicorn[standard]==0.52.0
websockets==17.0
pydantic==2.13.4
